# Dependencies
node_modules/
package-lock.json

# Build artifacts
dist/
*.zip

# Superseded source files (not loaded by the manifest; delete locally with `git rm`)
content/refined.css
content/apple.css

# Sandbox scratch file (could not be deleted from the build environment)
_writetest.txt

# Large demo source files — only the cropped mp4 + poster are committed
clearer_hn_show.mp4
clearer_hn_show.gif
clearer_hn_show_cropped.gif

# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/
